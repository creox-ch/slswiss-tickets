/**
 * Заголовок по умолчанию — для всех страниц, у которых нет своего.
 *
 * Было «SoiLüDi — билеты (тест)»: домен общий, и по нему ходят продавцы
 * бренд-маркета — письмо об оплате пакета Frankenplatz ведёт сюда же. Человек,
 * только что заплативший 89–249 CHF, видел во вкладке чужой бренд и слово
 * «тест»: ровно то, как выглядит фишинг. Поддомена маркета не будет (решение
 * 20.08), поэтому заголовок нейтральный — он не должен противоречить ни одному
 * из продуктов, которые здесь живут.
 */
export const metadata = { title: 'Creox — билеты и кабинеты' };

export default function RootLayout({ children }) {
  return (
    <html lang="ru">
      <body style={{ margin: 0 }}>{children}</body>
    </html>
  );
}
