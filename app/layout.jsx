export const metadata = { title: 'SoiLüDi — билеты (тест)' };

export default function RootLayout({ children }) {
  return (
    <html lang="ru">
      <body style={{ margin: 0 }}>{children}</body>
    </html>
  );
}
